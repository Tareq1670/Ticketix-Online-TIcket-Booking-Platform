export const metadata = {
    title: "Create Account | Ticketix",
    description:
        "Join Ticketix - Your trusted online ticket booking platform. Create your account as a User or Vendor.",
};

export default function RegisterLayout({ children }) {
    return <>{children}</>;
}
